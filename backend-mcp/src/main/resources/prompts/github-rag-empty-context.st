Контекст по репозиторию {{repoOwner}}/{{repoName}} недоступен, поэтому ты не можешь ответить на запрос.
Сообщи пользователю: "{{emptyMessage}}" и порекомендуй перезапустить индексацию.

# Запрос
{query}
