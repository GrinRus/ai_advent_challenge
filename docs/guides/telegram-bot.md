# Telegram чат-бот AI Advent

## Основные сценарии
- `/start` — приветствие, сброс состояния, показ основного меню с текущими настройками.
- `/new` — явный сброс текущей сессии и истории перед новым запросом.
- `/menu` — отображает главное меню (провайдер, модель, режим, инструменты, sampling, новый диалог).
- Голосовое сообщение → распознаётся и автоматически отправляется как текстовый запрос (в ответ бот присылает расшифровку со значком `🎤`).

## Соответствие UI FE ↔ Telegram
| Функция веб-клиента | Реализация в Telegram |
| --- | --- |
| Выбор провайдера/модели | Inline-меню «Провайдер» → список провайдеров, «Модели» → модели выбранного провайдера (только sync-enabled). |
| Выбор режима (Streaming/Sync/Research/Structured) | В меню «Режим» доступны переключатели `Стандартный (sync)` и `Research (MCP)`. Стриминг закрывается отдельной задачей. |
| Выбор MCP-инструментов | Меню «Инструменты» показывает список доступных MCP (из конфигурации); активные отмечены `✅`, есть кнопка «Очистить». |
| Настройка sampling (temperature, topP, maxTokens) | Меню «Sampling» содержит преднастроенные значения (0.1–1.0, 0.5–1.0, 512–2048) и кнопку «Сбросить к дефолту». |
| Отображение usage/cost | После ответа приходит карточка «Ответ готов…» с кнопкой «Подробнее» (usage/cost), «Повторить», «Меню». |
| Structured response | При наличии структурированного payload появляется кнопка «Структура» (планируется на следующем шаге). |
| Остановка стрима | Inline-кнопка «Остановить» во время выполнения sync-запроса прекращает ожидание, отменяет future. |

## Пользовательский сценарий
1. `/start` → бот приветствует и открывает меню.
2. Пользователь настраивает провайдера/модель, режим, инструменты и sampling (при необходимости).
3. Отправляет текст или голосовое сообщение → бот распознаёт, отправляет в sync-конвейер, показывает прогресс («Отправил запрос…»).
4. После завершения приходит:
   - Ответ модели (plain text);
   - Метаданные (провайдер, модель, режим, latency, инструменты) с кнопками «Подробнее» (usage/cost), «Повторить», «Меню»;
   - При голосовом сообщении дополнительно возвращается расшифровка `🎤 …`.
5. «Меню» позволяет изменить настройки в любой момент. Изменение провайдера/модели/режима сбрасывает sessionId.

## Команды и inline-кнопки
- `/start`, `/new`, `/menu`.
- Inline: `Провайдер`, `Модели`, `Режим`, `Инструменты`, `Sampling`, `Новый диалог`, `Очистить` (инструменты), «Остановить», «Повторить», «Подробнее», «Меню».

## Голосовые сообщения
- Включаются через `app.telegram.stt.enabled=true`.
- Настраиваются модели `TELEGRAM_STT_MODEL` (по умолчанию `gpt-4o-mini-transcribe`) и `TELEGRAM_STT_FALLBACK_MODEL`.
- Расшифровка выполняется через `OpenAiAudioTranscriptionModel`, язык (`TELEGRAM_STT_LANGUAGE`, по умолчанию `ru`).
- При ошибке загрузки/распознавания пользователь получает уведомление и может повторить попытку.

## Задачи для публикации/тестирования
- Обновить README / product-гайды (этот документ).
- Готовится чек-лист для релиза Wave 27 с включением Telegram.
- Планируется расширить structured output, добавить интеграционные тесты и сценарии деградации.
