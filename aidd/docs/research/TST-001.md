# Research Summary — TST-001: FS-RBAC-03 Live RBAC Enforcement

Status: ready
Last reviewed: 2026-03-18
Ticket: TST-001
Slug: fs-rbac-03-live-enforcement

## AIDD:CONTEXT_PACK
- TBD
- Limit: <= 20 lines / <= 1200 chars.
- Paths discovered: none
- Invalid paths: none
- Pack-first: используйте `*-rlm.pack.*` и `rlm-slice`; не вставляйте raw JSONL.

## AIDD:PRD_OVERRIDES
- none
## AIDD:NON_NEGOTIABLES
- All POST/PUT/DELETE APIs must enforce RBAC with 403 on violations
- Role operations (GRANT/REVOKE) must be auditable with timestamp, user, action
- Frontend role guards must update within 5 seconds of session change
- RBAC check latency P95 ≤ 10ms

## AIDD:OPEN_QUESTIONS
None - all questions resolved. See AIDD:DECISIONS for finalized answers.

## AIDD:RISKS
| Risk | Impact | Mitigation |
|------|--------|------------|
| No Spring Security in current classpath | High | Add spring-boot-starter-security dependency |
| No JWT infrastructure exists | Medium | Implement custom RBAC based on profile roles from header |
| Profile auth uses custom headers not JWT claims | Medium | Adapt RBAC to use X-Profile-Key header for identity |
| SSE exists but PRD requires WebSocket | Low | Evaluate SSE for role push or implement WebSocket |
| Database schema has no audit_log table | Medium | Create new migration for audit_log table |

## AIDD:DECISIONS
- **Auth Model**: Current system uses X-Profile-Key and X-Profile-Auth headers, not JWT
- **State Management**: Frontend uses Zustand (not Redux) with persistence
- **Real-time**: SSE already implemented for flows/MCP; Polling chosen over WebSocket for simpler implementation
- **Role Storage**: Roles stored in profile document (see profileTypes.ts: `roles: z.array(z.string())`)
- **Q1 (RBAC Implementation)**: Custom WebFlux filters (Spring Security not added to classpath - using existing header-based auth)
- **Q2 (Profile-to-role mapping)**: Extract roles from profile service using X-Profile-Key header
- **Q3 (Real-time mechanism)**: Polling with 30s interval (not SSE/WebSocket) for role sync

## AIDD:INTEGRATION_POINTS

### Backend Integration Points

1. **Profile Header Extraction** (`/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/lib/profileStore.ts:92-110`)
   - `buildActiveProfileHeaders()` creates headers: X-Profile-Key, X-Profile-Channel, X-Profile-Auth
   - Backend must read these headers for identity

2. **API Client** (`/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/lib/apiClient.ts`)
   - All API calls use `buildActiveProfileHeaders()` for authentication
   - Admin role APIs: `fetchAdminProfiles()`, `assignProfileRole()`, `revokeProfileRole()`

3. **No Existing Security Configuration**
   - No SecurityConfig, JWT, or @PreAuthorize annotations found
   - No spring-boot-starter-security in build.gradle

### Frontend Integration Points

1. **Profile Store** (`/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/lib/profileStore.ts`)
   - Uses Zustand with persistence
   - Profile document includes `roles: string[]`
   - Admin check: `state.profile?.roles?.some((role) => role === 'admin')`

2. **Admin Role Guard** (`/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/pages/AdminRoles.tsx:34-36`)
   ```typescript
   const hasAdminRole = useProfileStore(
     (state) => !!state.profile?.roles?.some((role) => role === 'admin'),
   );
   ```

3. **Route Guard** (`/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/App.tsx:41-45`)
   ```typescript
   {hasAdminRole && (
     <NavLink to="/admin/roles" className="nav-link">
       Admin
     </NavLink>
   )}
   ```

4. **SSE Infrastructure** (`/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/lib/apiClient.ts`)
   - `subscribeToFlowEvents()` uses EventSource for real-time updates
   - Could be extended for role change notifications

## AIDD:REUSE_CANDIDATES

1. **Profile Header Parsing**
   - Location: `buildActiveProfileHeaders()` in `/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/lib/profileStore.ts:92-110`
   - Reuse: Backend should implement similar header parsing for identity extraction

2. **Role Check Pattern**
   - Location: `/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/pages/AdminRoles.tsx:34-36`
   - Pattern: `state.profile?.roles?.some((role) => role === 'admin')`
   - Reuse: Standardize role check utility

3. **Audit Event Types**
   - Location: `/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/components/ActiveProfileBanner.tsx:77-95`
   - Events: 'profile_created', 'profile_updated', 'identity_attached', 'identity_detached', 'role_assigned', 'role_revoked'
   - Reuse: Extend for RBAC audit events

4. **SSE Event Handling**
   - Location: `/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/pages/FlowSessions.tsx`
   - Pattern: EventSource with fallback to polling
   - Reuse: Adapt for role change push notifications

## AIDD:COMMANDS_RUN
```bash
# Search for auth/security patterns
grep -r -i "websocket\|socket.io\|sse\|event-source\|EventSource" frontend/src/
grep -r -i "jwt\|spring-security\|SecurityFilter\|@PreAuthorize\|@Secured" backend-mcp/src/
grep -r -i "role\|permission\|admin\|RBAC" backend-mcp/src/main/java/

# File discovery
glob **/*Security*.java
glob **/*Auth*.java
glob **/*Config*.java
```

## AIDD:RLM_EVIDENCE
- Status: ready
- Pack: aidd/reports/research/TST-001-rlm.pack.json
- Pack status: found
- Pack bytes: 11279
- Pack updated_at: 2026-03-18T10:44:48Z
- Links: Built and finalized (377 nodes)
- Auto-heal: Completed via rlm_links_build + rlm_finalize
- Baseline marker: links_empty_reason=no_symbols
- Auto recovery: auto_recovery_attempted=yes, bootstrap_attempted=yes, finalize_attempted=yes, recovery_path=bootstrap_nodes
- Slice: python3 ${CLAUDE_PLUGIN_ROOT}/skills/aidd-rlm/runtime/rlm_slice.py --ticket TST-001 --query "<token>"
- Nodes/links: aidd/reports/research/TST-001-rlm.nodes.jsonl / aidd/reports/research/TST-001-rlm.links.jsonl (не читать целиком)

## AIDD:TEST_HOOKS
- Frontend tests: `/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/lib/profileStore.test.ts`
- No backend security tests found
- Suggested: Add RBAC middleware tests, audit logging tests

## Context Pack (TL;DR)
- **Entry points**: Backend MCP server on port 8080 (configurable via profiles)
- **Reuse candidates**: Profile header auth, Zustand role checks, SSE infrastructure
- **Integration points**: X-Profile-Key header parsing, role assignment/revocation APIs
- **RLM**: No existing security patterns found (greenfield RBAC implementation)
- **Test pointers**: profileStore.test.ts for frontend state testing
- **Top risks**: No Spring Security, no JWT, custom auth model

## Definition of reviewed
- [x] Найдены ≥ 1 точки интеграции или явно указан baseline
- [x] Указаны тесты/контракты или явно зафиксирован риск «нет тестов»
- [x] Зафиксированы команды/пути сканирования и ссылки на отчёты

## Контекст
- **Цель фичи**: Implement live RBAC enforcement for all mutation APIs (POST/PUT/DELETE) with audit logging and frontend sync
- **Scope изменений**: Backend middleware, audit database schema, frontend real-time sync
- **Ключевые модули/директории**:
  - Backend: `backend-mcp/src/main/java/com/aiadvent/mcp/backend/` - no security module exists
  - Frontend: `frontend/src/lib/profileStore.ts`, `frontend/src/pages/AdminRoles.tsx`
- **Исходные артефакты**: PRD TST-001, Context Pack TST-001
- **Логи команд / отчёты**: RLM worklist shows no existing security patterns

## Точки интеграции

### Backend
1. **New RBAC Middleware** (to be created)
   - Location: `backend-mcp/src/main/java/com/aiadvent/mcp/backend/security/`
   - Integration: Spring WebFlux filter chain (WebFlux already in classpath)
   - Headers to read: X-Profile-Key, X-Profile-Auth

2. **Audit Log Table** (new migration needed)
   - Location: `backend-mcp/src/main/resources/db/changelog/rbac/`
   - Schema: timestamp, user_id (profile_key), action, target_resource, result

3. **Profile Service Integration**
   - Current: Profile roles returned via API (`/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/lib/profileTypes.ts:31`)
   - Integration: Backend needs to validate roles against profile service or cache

### Frontend
1. **Profile Store Extension** (`/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/lib/profileStore.ts`)
   - Add: WebSocket/SSE listener for role changes
   - Current: `applyProfileSnapshot()` updates profile including roles (line 57-64)

2. **API Client** (`/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/lib/apiClient.ts`)
   - Already has: `subscribeToFlowEvents()` for SSE
   - Add: Role change subscription endpoint

## Повторное использование

### 1. Header-based Authentication
- **Path**: `/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/lib/profileStore.ts:92-110`
- **Usage**: Backend filter should parse X-Profile-Key header format: `{namespace}:{reference}`
- **Risk**: Custom auth not standardized; document contract clearly
- **Tests**: No existing tests for header parsing

### 2. Zustand Role State Pattern
- **Path**: `/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/pages/AdminRoles.tsx:34-36`
- **Usage**: `useProfileStore((state) => !!state.profile?.roles?.some((role) => role === 'admin'))`
- **Risk**: Role check scattered across components; create reusable hook
- **Tests**: Component-level tests needed

### 3. SSE Infrastructure
- **Path**: `/Users/griogrii_riabov/grigorii_projects/ai_advent_challenge_new/frontend/src/lib/apiClient.ts` (subscribeToFlowEvents)
- **Usage**: Extend for role change notifications or implement WebSocket
- **Risk**: SSE is unidirectional; role changes may need bidirectional comms
- **Tests**: FlowSessions.tsx has SSE usage patterns

## Принятые практики
- Profile headers: X-Profile-Key, X-Profile-Channel, X-Profile-Auth (see profileStore.ts)
- Role storage: Array of strings in profile document (profileTypes.ts:31)
- Audit events: Structured with eventType, metadata, timestamp (ActiveProfileBanner.tsx:77-95)

## RLM evidence
- **No existing security modules found** - greenfield implementation required
- **Database entities exist**: NoteEntity with user_namespace/user_reference pattern
- **Header-based auth established**: X-Profile-Key pattern used across frontend

## Паттерны/анти-паттерны
- **Паттерны**:
  - Header-based service-to-service auth (FlowOpsClientConfiguration.java:27-29)
  - Zod schema validation for API responses (apiClient.ts:187-194)
  - Audit event typing with metadata (profileTypes.ts:38-47)
- **Анти-паттерны**:
  - No centralized auth/authorization layer
  - Role checks inline in components (AdminRoles.tsx, App.tsx)
  - No standardized 403 handling

## Отсутствие паттернов / отсутствующие данные
- **No Spring Security**: spring-boot-starter-security not in build.gradle
- **No JWT libraries**: No jjwt or similar dependencies
- **No @PreAuthorize annotations**: No method-level security
- **No WebSocket**: No spring-boot-starter-websocket
- **No audit_log table**: Only note_entry and repo_rag_* tables exist

## Gap-анализ

| Gap | Ограничение | Компенсационный шаг |
|-----|-------------|---------------------|
| No Spring Security | Must add dependency | Add spring-boot-starter-security to build.gradle |
| Custom header auth vs JWT | PRD assumes JWT claims | Adapt RBAC to extract roles from profile service using X-Profile-Key |
| No WebSocket | PRD requires WebSocket push | Evaluate SSE extension first (lower complexity) |
| No audit table | Need audit_log schema | Create Liquibase migration for audit_log table |
| Zustand vs Redux | PRD mentions Redux | Update PRD to reflect actual Zustand usage |

## Следующие шаги
1. **Update PRD**: Change Q2 answer from JWT-claims to custom header-based auth
2. **Add Dependencies**: spring-boot-starter-security, spring-boot-starter-websocket (if WebSocket chosen)
3. **Create Database Migration**: audit_log table with Liquibase
4. **Implement RBAC Filter**: Spring WebFlux filter for POST/PUT/DELETE endpoints
5. **Extend Profile Store**: Add real-time role sync (SSE or WebSocket)
6. **Create Audit Service**: Log GRANT/REVOKE/DENIED operations

## Дополнительные заметки

### Current Database Schema (PostgreSQL)
- **Existing tables**: note_entry, note_vector_store, repo_rag_*
- **Pattern**: UUID primary keys, JSONB for metadata, created_at/updated_at timestamps
- **User identification**: user_namespace + user_reference (see NoteEntity.java:40-44)

### Frontend Dependencies
- **State**: Zustand (not Redux)
- **HTTP**: Native fetch
- **Validation**: Zod
- **Real-time**: EventSource (SSE) for flows

### Backend Dependencies (build.gradle)
- **Web**: spring-boot-starter-webflux
- **Data**: spring-boot-starter-data-jpa, postgresql
- **No security**: spring-boot-starter-security missing
- **No WebSocket**: spring-boot-starter-websocket missing

### Profile API Contract
- **Headers**: X-Profile-Key (format: `namespace:reference`), X-Profile-Auth (dev token)
- **Role format**: Array of strings (`roles: ["admin", "user"]`)
- **Admin check**: Presence of `"admin"` in roles array

## Открытые вопросы
None - all resolved. See AIDD:DECISIONS for answers:
- **Q1**: Custom WebFlux filters (not Spring Security)
- **Q2**: Polling (not SSE/WebSocket)
- **Q3**: Call profile service on each request (with caching)
- **Q4**: Profile service is source of truth
